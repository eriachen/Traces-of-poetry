# Railway build configuration
[phases.setup]
  nixpkgsArchive = "2024-01-01"
  
[phases.install]
  dependsOn = ["setup"]
  cmds = ["npm install"]

[phases.build]
  dependsOn = ["install"]
  cmds = ["cd backend && npm install && npm run build"]

[start]
  cmd = "cd backend && npm start"
